#include <stdio.h>
#include "../Project1/Terminal/terminal.h"
#include "../Project1/Application/app.h"

void main()
{
	appStart();
}
/*isCardExpired(&customer1, &terminal1);
		getTransactionAmount(&terminal1);
		setMaxAmount(&terminal1, maxAmount);
		isBelowMaxAmount(&terminal1);*/